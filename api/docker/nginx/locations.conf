# Shared request routing, included by both the :80 and :443 server blocks in
# app.conf. Kept in one file so the two can never drift apart — a route added
# for HTTP but forgotten for HTTPS is exactly the bug this prevents.

root /var/www/html/public;
index index.php;

charset utf-8;
client_max_body_size 60m;   # keep in step with php.ini post_max_size

access_log /dev/stdout;
error_log  /dev/stderr warn;

# Websockets: Reverb speaks the Pusher protocol under /app/{key}.
location ~ ^/(app|apps)/ {
    proxy_pass http://reverb;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 3600s;   # idle sockets must survive a quiet night
    proxy_send_timeout 3600s;
}

# User files, served off disk when `artisan storage:link` has been run.
#
# Cloudflare proxies this origin and caches by file extension with a 4-hour edge
# TTL — verified here: a .bin placed under /storage kept serving from the edge as
# a HIT well after it had been deleted from the origin. Nothing user-owned is
# reachable through this path today (files go through /api and are authenticated),
# but a single `storage:link` would be enough to publish private uploads to a
# public CDN. The header is set here and not on the /api block below because
# Laravel already sends its own Cache-Control there, and nginx's add_header
# appends rather than replaces — two headers where one will do.
location ^~ /storage {
    add_header Cache-Control "private, no-store" always;
    try_files $uri /index.php?$query_string;
}

# Laravel: API, broadcasting auth, health probe, signed downloads.
location ~ ^/(api|broadcasting|up)(/|$) {
    try_files $uri /index.php?$query_string;
}

location = /index.php {
    include fastcgi_params;
    fastcgi_pass php-fpm;
    fastcgi_param SCRIPT_FILENAME $document_root/index.php;
    fastcgi_param HTTPS $http_x_forwarded_proto;
    fastcgi_read_timeout 300;   # long enough for an export or a merge
    fastcgi_buffers 16 16k;
    fastcgi_buffer_size 32k;
}

# Nothing else may reach PHP directly.
location ~ \.php$ {
    return 404;
}

# FPM's own status/ping endpoints stay inside the network.
location ~ ^/(fpm-status|fpm-ping)$ {
    deny all;
}

# Everything else is the Next.js app.
location / {
    proxy_pass http://nextjs;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location ~ /\.(?!well-known) {
    deny all;
}
